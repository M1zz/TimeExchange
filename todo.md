# TimeExchange — 시간의 색 재구성

데모(장부 연결 HTML)를 iOS 앱에 반영. 색칠판이 메인이 되고, 초록 칸 출금이 기존 환전소(Exchange)와 물리적으로 연결된다.

## 회계 원칙 (구현 기준)

- 네 숫자를 **합산하지 않는다.** 주간 읽기는 5줄, 총점 없음.
- 노랑(팔았다): 입금액 + 태그 → 실측 칸당 단가. 단가가 갈리면 "어느 노랑을 줄일지"가 보인다.
- 파랑(심었다): **금액 입력란 없음.** 태그만. 회수는 별도 섹션에서 소급 배분.
- 빨강(누렸다): 원가만. 가치는 계산하지 않는다.
- 초록(돌봤다): 운영비 + 출금 거래. 지불액으로 칸을 사서 다른 색으로 바꾼다.
- 안개: 전표를 붙일 수 없다. 개수만 센다.

## 작업

- [x] 기존 앱 구조 파악 (SwiftUI + SwiftData, Exchange 모델)
- [x] 범위 확정: 색칠판 중심 재구성 (탭 = 색칠판 / 주간 읽기 / 환율)
- [x] Theme.swift — 데모 팔레트
- [x] Models.swift — TimeColor, Slot, TimeCell, Recovery, Exchange 확장
- [x] WeeklyReading.swift — 5줄 집계 (합산 없음)
- [x] LoomView.swift — 주간 그리드, 칠하기/전표 모드, 드래그 칠하기
- [x] LedgerSheets.swift — 색마다 다른 전표 시트
- [x] WeeklyReadingView.swift — 5줄 + 파랑 회수 섹션
- [x] ContentView.swift — 3탭
- [x] 기존 TodayView / HistoryView / AddExchangeSheet 제거
- [x] SeedData — 데모 시드 (멘토링 12만 / 외주 20만 / 강의 9만, devkoan 글 18만 회수)
- [x] 색 변경 시 전표 리셋 **확인 단계** (데모의 알려진 결함 보완)
- [x] 칠하기/전표 모드 제거 — 칸 탭 하나로 통합, 색은 전표 시트 안에서 고름
- [x] 금액 입력 시 한글 읽기 표시 (1000000 → 백만원). `Fmt.korean`
- [x] 다른 곳을 누르면 키보드 내려감 + 숫자 키패드 위 «완료» 버튼
- [x] 라이트/다크 두 벌 팔레트, 시스템 외형 따라감
- [x] 색칠판에 물감 팔레트 복귀 — 모드 스위치가 아니라 «손에 든 것». 기본은 전표
- [x] 드래그로 여러 칸 칠하기 (전표 붙은 칸은 건너뛰고 손 뗀 뒤 확인)
- [x] «개념» 탭 — 색마다 전표가 왜 다르게 생겼는지 설명
- [x] 본문 글자 최소 .body로 상향
- [x] 월급 칸 — 노랑 전표에 «이 칸은 월급에서 나옵니다» 토글. 금액 대신 그 달 월급을 배분
- [x] 판 시간 시급 vs 구속 시급 — 두 숫자를 나란히, 쓰임을 갈라 놓음
- [x] 달 중간에는 경과일수로 안분 (전액을 몇 칸에 나누면 값이 부풀려짐)
- [x] 회수를 세 종류로 — 돈 / 시간 / 접음. «접음»이 없으면 장부가 성공한 것만 기억한다
- [x] 배당 연결 — 빨강 칸에 «어느 자산에서 나왔나». 값은 안 매기고 세기만
- [x] «자산» 탭 — 태그별 장부. 심은 지 N주, 돌아온 것 세 줄, 상태 문장
- [x] 빌드 확인

## 설계 메모

모드 두 개는 데모(마우스, 드래그)의 산물이라 앱에서는 없앴다. 칸을 누르면 전표가 열리고
색은 그 안에서 고른다. 색을 정하는 것과 전표를 쓰는 것이 한 번의 판단이므로 화면도 하나다.

데모에서는 색을 바꾸면 전표가 조용히 사라진다. 계정과목이 바뀌면 전표 이월이 회계상
틀리므로 리셋 자체는 맞지만, 실수로 지울 수 있는 구조다. 통합 시트에서는 색을 바꾸는
순간 경고가 뜨고, 저장을 눌러야 실제로 지워진다. 원래 색으로 되돌리면 전표도 돌아온다.

물감은 다시 돌아왔지만 모드 스위치로는 돌아오지 않았다. 팔레트의 첫 칸이 «전표»이고
그게 기본값이다. 물감을 집으면 칠하기가 되고, 같은 물감을 다시 누르면 내려놓는다.
"지금 손에 무엇을 들고 있는가"는 화면에 보이지만 "지금 무슨 모드인가"는 안 보인다.

## 확인한 것 (시뮬레이터 iPhone 17)

- 색칠판: 시드 한 주가 그려지고 전표 붙은 칸에 점이 찍힘
- 노랑 전표: 입금액 120,000 / 태그 멘토링 프리필
- 파랑 전표: 금액 칸 없음, 태그 + 기존 태그 칩만
- 초록 전표: 운영비 + 지불액 + 심었다로/누렸다로 + 돌려받은 시간(칸 길이 기본값)
- 주간 읽기: 다섯 줄, 총점 없음. devkoan 글 18만 회수가 칸당 18만으로 소급됨

## 남은 것

- [ ] «출근» 표시 — 지금은 노랑만 월급 칸으로 잡히므로, 출근 중에 파랑·초록·안개로 칠한 시간이
      급여받은 시간이었는지 알 수 없다. 칸에 색과 별개로 «회사에 있었나»를 달면
      "출근 40시간 = 노랑 20 / 파랑 10 / 초록 6 / 안개 4" 같은 읽기가 가능해진다.

- [ ] 위젯 / 알림 등 기록 유도 장치는 아직 없음
